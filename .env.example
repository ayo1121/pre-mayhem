# ══════════════════════════════════════════════════════════════════════════════
# PUMPFUN AGE STREAK BOT - CONFIGURATION
# ══════════════════════════════════════════════════════════════════════════════
#
# Copy this file to .env and fill in your values:
#   cp .env.example .env
#
# ⚠️  NEVER commit your .env file to git!
# ⚠️  NEVER share your treasury.json keypair!
#
# ══════════════════════════════════════════════════════════════════════════════

# ─────────────────────────────────────────────────────────────────────────────
# REQUIRED - You MUST set these
# ─────────────────────────────────────────────────────────────────────────────

# Solana RPC endpoint (Helius recommended for best parsing)
# Get a free key at https://helius.dev
RPC_URL=

# Helius API key (used for enhanced transaction parsing)
# Same key from your Helius dashboard
HELIUS_API_KEY=

# Token mint address (pump.fun or any SPL token)
# This is the token the bot will buy and distribute
TOKEN_MINT=

# Absolute path to your treasury wallet keypair JSON file
# Generate with: solana-keygen new -o treasury.json
# ⚠️  Keep this file secure and NEVER commit it!
TREASURY_KEYPAIR_PATH=

# ─────────────────────────────────────────────────────────────────────────────
# SAFETY - Start with DRY_RUN=true!
# ─────────────────────────────────────────────────────────────────────────────

# Set to true for testing (no real transactions)
# ⚠️  Only set to false when you're ready for production!
DRY_RUN=true

# ─────────────────────────────────────────────────────────────────────────────
# SCHEDULING
# ─────────────────────────────────────────────────────────────────────────────

# How often to run the buy job (seconds)
# Default: 3600 = 1 hour
BUY_INTERVAL_SECONDS=3600

# How often to run the reward job (seconds)
# Default: 7200 = 2 hours
REWARD_INTERVAL_SECONDS=7200

# ─────────────────────────────────────────────────────────────────────────────
# ELIGIBILITY - Tune these for your token economics
# ─────────────────────────────────────────────────────────────────────────────

# Minimum wallet age in days (anti-sybil)
WALLET_MIN_AGE_DAYS=90

# Minimum holding continuity in seconds
MIN_CONTINUITY_SECONDS=7200

# Minimum SOL spent on buys to be eligible
MIN_CUMULATIVE_BUY_SOL=0.1

# Number of winners per reward round
WINNERS_PER_ROUND=10

# ─────────────────────────────────────────────────────────────────────────────
# TREASURY SPEND
# ─────────────────────────────────────────────────────────────────────────────

# SOL to reserve for transaction fees
SOL_FEE_RESERVE=0.03

# Minimum SOL to spend on buy (skip if below)
MIN_BUY_SOL=0.01

# Maximum SOL per buy (safety cap)
MAX_BUY_SOL_PER_HOUR=0.2

# Jupiter slippage tolerance in basis points (300 = 3%)
JUPITER_SLIPPAGE_BPS=300

# ─────────────────────────────────────────────────────────────────────────────
# REWARD POLICY
# ─────────────────────────────────────────────────────────────────────────────

# Percentage of treasury tokens to distribute (basis points, 9000 = 90%)
REWARD_TOKEN_PERCENT_BPS=9000

# Maximum percentage per round (safety cap, 3000 = 30%)
MAX_REWARD_TOKEN_PERCENT_PER_ROUND=3000

# Maximum token transfers per transaction
MAX_SENDS_PER_TX=6

# ─────────────────────────────────────────────────────────────────────────────
# INDEXING
# ─────────────────────────────────────────────────────────────────────────────

# Signatures to fetch during bootstrap
BOOTSTRAP_SIGNATURE_LIMIT=5000

# Signatures per incremental scan tick
SIGNATURE_SCAN_LIMIT_PER_TICK=1000

# ─────────────────────────────────────────────────────────────────────────────
# STATUS API (for frontend timers)
# ─────────────────────────────────────────────────────────────────────────────

# Port for the read-only status server
STATUS_SERVER_PORT=3001

# Allowed origin for CORS (your frontend domain)
# Use * for development, set to your Vercel URL in production
STATUS_ALLOWED_ORIGIN=
